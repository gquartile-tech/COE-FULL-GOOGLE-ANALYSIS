services:
  - type: web
    name: google-coe-full-analysis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 500 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
